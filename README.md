# Booking-Backend
A RESTful backend for a booking application built with Node.js and Express, featuring JWT-based authentication, bcrypt password hashing, and MongoDB (Mongoose) for data management. Follows MVC architecture with middleware, validation, and centralized error handling.
